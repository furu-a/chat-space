.comment{"data-id": message.id}
  %p.comment_creata
    = message.user.name
    %time.time
      = message.created_at
  %p.comment_text
    - if message.content.present?
      = message.content
  = image_tag message.image.url, html: {class: 'image'} if message.image.present?
